invocation:

FLASK_APP=wordlist.py FLASK_DEBUG=1 CONFIG=config.Config python -m flask run

to do:

in word form, show all fields, even those for which there is no attr values
error handling
navigation
